# Скопируйте в .env (backend/.env) и подставьте свои значения
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/metka
# Любая длинная случайная строка, например: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me
